export type Variables = {
  userId: string;
  userRole: string;
};

export type PriceEntry = {
  rate: number;
  ts: number;
};

export type userdata = {
  id: string;
  email: string;
  password_hash: string;
  role: string;
  created_at: Date;
};
